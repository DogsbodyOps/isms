# To Update
Edit file and dont forget to add the seperating % and trailing %
strfile -c % <fortunefile> <fortunefile>.dat
Copy file to /usr/share/games/fortune/<fortunefile>

## Quick Deploy
```curl -fsSL https://raw.githubusercontent.com/DogsbodyOps/isms/refs/heads/main/isms_deploy.sh | bash```

add '''fortune isms | cowsay''' to ~/.bashrc to have it start at login.
